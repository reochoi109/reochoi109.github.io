# `theme` branch

This branch is for maintaining the custom Hugo theme.

- Theme source lives in `themes/studio/**`.
- Keep site content and deployment workflow out of this branch (those belong to `main`).

## Local dev / smoke test

```bash
./scripts/theme-smoke-test.sh
```

