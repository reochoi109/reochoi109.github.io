+++
title = "데이터베이스"
description = "데이터 모델링, 인덱스와 트랜잭션"
layout = "interview"
type = "page"
standalone = true
index = 5
robots = "noindex, nofollow, noarchive"

[build]
list = "local"
publishResources = false
render = "always"

[sitemap]
disable = true

[[cards]]
subcategory = "데이터 모델링"
difficulty = "초급"
question = "기본 키와 유니크 키의 차이는 무엇인가요?"
answer = "기본 키는 행을 대표해 식별하는 키로 테이블당 하나이며 NULL을 허용하지 않습니다. 유니크 제약은 지정한 값의 중복을 막으며 한 테이블에 여러 개 둘 수 있습니다."
explanation = "DBMS마다 유니크 컬럼에서 NULL을 다루는 규칙은 다를 수 있습니다. 자연 키가 변경 가능하거나 크다면 내부 식별용 대리 키를 기본 키로 두고, 업무상 고유해야 하는 값에는 별도 유니크 제약을 두는 방식을 자주 사용합니다."
points = ["기본 키는 엔터티의 대표 식별자다.", "업무상 고유성은 DB 제약으로 보장한다.", "애플리케이션 확인만으로 중복을 막으면 경쟁 조건이 생긴다."]
terms = [{ name = "기본 키", description = "테이블의 각 행을 유일하게 식별하는 대표 키입니다." }, { name = "유니크 제약", description = "지정한 컬럼 조합의 중복 값을 데이터베이스가 거부하도록 하는 제약입니다." }]

[[cards]]
subcategory = "인덱스"
difficulty = "중급"
question = "B-Tree 인덱스가 조회 성능을 높이는 원리를 설명해보세요."
answer = "키를 정렬된 균형 트리로 관리해 루트에서 리프까지 탐색 범위를 단계적으로 줄입니다. 전체 테이블을 순차 탐색하지 않고 원하는 키나 범위의 행 위치를 찾을 수 있습니다."
explanation = "높은 fan-out 덕분에 트리 높이가 낮고 리프 노드가 정렬되어 범위 검색에 유리합니다. 대신 쓰기 시 인덱스도 갱신해야 하고 저장 공간을 사용합니다. 복합 인덱스는 선두 컬럼과 조건 형태가 중요하며 실행 계획으로 실제 사용 여부를 확인해야 합니다."
points = ["등가 검색과 범위 검색에 적합하다.", "선택도가 낮으면 인덱스 이점이 작을 수 있다.", "읽기 향상과 쓰기 비용 사이의 트레이드오프다."]
terms = [{ name = "B-Tree", description = "정렬된 키를 여러 자식 노드로 나누어 낮은 높이를 유지하는 균형 트리입니다." }, { name = "선택도", description = "조건이 전체 행 중 얼마나 적은 행을 골라내는지를 나타내는 정도입니다." }]
image = "/images/study/btree-index.svg"
image_alt = "루트와 내부 노드, 리프 노드로 구성된 B-Tree 인덱스 탐색 구조"
image_caption = "B-Tree가 검색 범위를 단계적으로 줄이는 과정"

[[cards]]
subcategory = "트랜잭션"
difficulty = "고급"
question = "트랜잭션 격리 수준과 발생 가능한 읽기 현상을 설명해보세요."
answer = "격리 수준은 동시 트랜잭션이 서로의 변경을 관찰하는 범위를 정합니다. 낮은 수준에서는 Dirty Read, Non-repeatable Read, Phantom Read가 발생할 수 있고 수준을 높일수록 더 강한 일관성을 제공합니다."
explanation = "Serializable은 직렬 실행과 같은 결과를 목표로 하지만 충돌과 재시도 비용이 커질 수 있습니다. 실제 동작은 DBMS의 MVCC와 잠금 구현에 따라 다릅니다. 격리 수준 이름만 믿지 말고 필요한 불변식, 쿼리 패턴과 해당 DB의 보장 범위를 확인해야 합니다."
points = ["격리 수준과 원자성은 다른 개념이다.", "MVCC는 읽기와 쓰기의 충돌을 줄인다.", "강한 격리는 동시성과 재시도 비용을 높일 수 있다."]
terms = [{ name = "격리 수준", description = "동시 트랜잭션이 서로의 변경을 어느 범위까지 관찰할지 정하는 수준입니다." }, { name = "MVCC", description = "여러 버전의 행을 유지해 읽기와 쓰기의 충돌을 줄이는 동시성 제어 방식입니다." }, { name = "Phantom Read", description = "같은 범위 조건을 다시 조회했을 때 다른 트랜잭션의 삽입·삭제로 행 집합이 달라지는 현상입니다." }]

[[cards]]
subcategory = "데이터 모델링"
difficulty = "중급"
question = "정규화의 목적과 반정규화를 고려하는 시점을 설명해보세요."
answer = "정규화는 데이터 중복과 삽입·수정·삭제 이상을 줄이기 위해 함수적 종속성을 기준으로 테이블을 분해하는 과정입니다. 반정규화는 측정된 조회 병목을 줄이기 위해 중복 컬럼이나 집계 테이블을 의도적으로 도입하는 선택입니다."
explanation = "정규화된 모델을 기본으로 정확한 원본 데이터를 유지한 뒤 실제 쿼리와 실행 계획을 측정해야 합니다. 반정규화하면 읽기는 빨라질 수 있지만 여러 복사본의 일관성, 갱신 비용과 복구 절차가 필요합니다. 캐시나 인덱스로 해결 가능한지도 먼저 검토합니다."
points = ["정규화는 중복 자체보다 갱신 이상을 줄이는 것이 핵심이다.", "반정규화는 측정된 읽기 요구를 근거로 한다.", "중복 데이터의 일관성 유지 전략이 반드시 필요하다."]
terms = [{ name = "함수적 종속성", description = "한 속성 집합의 값이 다른 속성 값을 유일하게 결정하는 관계입니다." }, { name = "갱신 이상", description = "중복 데이터 때문에 삽입·수정·삭제 시 불일치나 불필요한 제약이 생기는 문제입니다." }, { name = "반정규화", description = "조회 성능이나 편의를 위해 중복을 의도적으로 허용하는 모델링 방식입니다." }]

[[cards]]
subcategory = "데이터 모델링"
difficulty = "고급"
question = "파티셔닝과 샤딩의 차이와 설계 기준을 설명해보세요."
answer = "파티셔닝은 큰 논리 테이블을 키 범위나 해시 기준으로 여러 물리 구역에 나누며 보통 하나의 데이터베이스 시스템이 관리합니다. 샤딩은 데이터를 여러 독립 데이터베이스 노드에 분산해 저장 용량과 처리량을 수평 확장합니다."
explanation = "파티션 키는 자주 사용하는 필터와 데이터 수명 주기를 고려하고, 샤드 키는 부하와 데이터가 고르게 분산되면서 주요 쿼리가 한 샤드에서 끝나도록 선택합니다. 잘못된 키는 Hotspot과 크로스 샤드 조인을 만들며 재샤딩은 큰 운영 비용이 듭니다."
points = ["파티셔닝은 관리·Pruning, 샤딩은 노드 간 수평 확장이 핵심이다.", "키 분포와 주요 접근 패턴을 함께 분석한다.", "분산 트랜잭션과 재분배 비용을 고려한다."]
terms = [{ name = "Partition Pruning", description = "조건과 관계없는 파티션을 읽지 않아 스캔 범위를 줄이는 최적화입니다." }, { name = "샤드 키", description = "각 행을 어느 데이터베이스 노드에 저장할지 결정하는 키입니다." }, { name = "Hotspot", description = "특정 파티션이나 노드에 요청과 데이터가 과도하게 몰리는 현상입니다." }]

[[cards]]
subcategory = "인덱스"
difficulty = "초급"
question = "데이터베이스 인덱스는 무엇이며 왜 모든 컬럼에 만들지 않나요?"
answer = "인덱스는 특정 컬럼 값을 별도 자료구조로 정렬해 원하는 행의 위치를 빠르게 찾도록 돕습니다. 인덱스마다 저장 공간을 사용하고 쓰기 시 함께 갱신해야 하므로 불필요하게 많으면 INSERT·UPDATE·DELETE와 유지보수 비용이 커집니다."
explanation = "조회 조건, 정렬, 조인에서 자주 사용되고 선택도가 충분한 컬럼이 후보입니다. 데이터가 적거나 대부분의 행을 읽는 쿼리에서는 순차 스캔이 더 빠를 수 있습니다. 실행 계획과 실제 통계로 인덱스 사용 여부를 확인해야 합니다."
points = ["인덱스는 읽기를 위한 추가 자료구조다.", "쓰기와 저장 공간 비용을 지불한다.", "쿼리 패턴과 실행 계획을 기준으로 설계한다."]
terms = [{ name = "인덱스", description = "컬럼 값과 행 위치를 별도로 관리해 검색 범위를 줄이는 자료구조입니다." }, { name = "순차 스캔", description = "테이블의 행이나 페이지를 처음부터 순서대로 읽는 접근 방식입니다." }, { name = "실행 계획", description = "옵티마이저가 선택한 조인 순서와 스캔·인덱스 사용 전략입니다." }]

[[cards]]
subcategory = "인덱스"
difficulty = "고급"
question = "복합 인덱스의 컬럼 순서를 어떻게 결정하나요?"
answer = "등가 조건, 범위 조건, 정렬과 조인 패턴을 기준으로 자주 함께 사용되는 선두 컬럼을 결정합니다. B-Tree 복합 인덱스는 왼쪽부터 정렬되므로 선두 컬럼 조건이 없으면 뒤 컬럼만으로 효율적인 탐색이 어려울 수 있습니다."
explanation = "무조건 선택도가 높은 컬럼을 앞에 두는 규칙만으로는 부족합니다. 실제 WHERE 조건 조합, 범위 조건 뒤의 컬럼 활용 제한, ORDER BY와 커버링 가능성을 확인해야 합니다. 비슷한 복합 인덱스가 중복되지 않도록 쓰기 비용도 함께 관리합니다."
points = ["왼쪽 선두 컬럼부터 정렬 구조가 형성된다.", "등가·범위·정렬 조건 순서를 함께 고려한다.", "실제 쿼리의 EXPLAIN과 통계로 검증한다."]
terms = [{ name = "복합 인덱스", description = "두 개 이상의 컬럼을 정해진 순서로 묶어 만든 인덱스입니다." }, { name = "Leftmost Prefix", description = "복합 인덱스가 선두 컬럼부터 이어지는 조건에서 효과적으로 사용되는 성질입니다." }, { name = "커버링 인덱스", description = "쿼리에 필요한 컬럼을 인덱스만으로 제공해 테이블 접근을 줄이는 인덱스입니다." }]

[[cards]]
subcategory = "트랜잭션"
difficulty = "초급"
question = "트랜잭션의 ACID를 설명해보세요."
answer = "원자성은 작업 전체가 모두 반영되거나 모두 취소됨을, 일관성은 트랜잭션 전후 제약이 유지됨을 뜻합니다. 격리성은 동시 작업이 서로 간섭하는 범위를 제어하고, 지속성은 커밋된 결과가 장애 뒤에도 보존됨을 뜻합니다."
explanation = "ACID는 애플리케이션이 올바른 트랜잭션 경계를 잡고 데이터베이스의 제약과 격리 수준을 적절히 사용해야 확보됩니다. 일관성은 DB가 비즈니스 규칙을 자동으로 모두 이해한다는 의미가 아니며, 지속성은 WAL과 디스크 flush 정책의 영향을 받습니다."
points = ["원자성·일관성·격리성·지속성을 각각 구분한다.", "격리 수준은 성능과 동시성에 영향을 준다.", "비즈니스 불변식은 제약과 애플리케이션 로직으로 정의한다."]
terms = [{ name = "원자성", description = "트랜잭션 작업이 전부 성공하거나 전부 취소되는 성질입니다." }, { name = "격리성", description = "동시에 실행되는 트랜잭션의 중간 결과가 서로 보이는 정도를 제어하는 성질입니다." }, { name = "지속성", description = "커밋된 데이터가 시스템 장애 이후에도 보존되는 성질입니다." }]

[[cards]]
subcategory = "트랜잭션"
difficulty = "중급"
question = "낙관적 락과 비관적 락의 차이를 설명해보세요."
answer = "비관적 락은 충돌이 발생한다고 가정하고 데이터를 읽을 때 잠가 다른 트랜잭션의 수정을 막습니다. 낙관적 락은 충돌이 드물다고 가정하고 버전 번호나 조건부 UPDATE로 커밋 시 변경 여부를 확인하며 충돌하면 재시도합니다."
explanation = "비관적 락은 충돌이 잦고 작업이 짧을 때 단순하지만 대기와 교착 위험이 있습니다. 낙관적 락은 읽기 동시성이 높지만 충돌 시 사용자의 작업을 다시 처리해야 할 수 있습니다. 충돌 빈도, 트랜잭션 길이와 재시도 가능성을 기준으로 선택합니다."
points = ["비관적 락은 먼저 잠그고, 낙관적 락은 마지막에 충돌을 검사한다.", "낙관적 락에는 버전 조건을 원자적으로 확인하는 UPDATE가 필요하다.", "재시도는 중복 부작용 없이 설계한다."]
terms = [{ name = "비관적 락", description = "다른 트랜잭션의 충돌을 예상해 대상 행을 미리 잠그는 방식입니다." }, { name = "낙관적 락", description = "잠금 대신 버전이나 조건으로 변경 충돌을 나중에 감지하는 방식입니다." }, { name = "조건부 UPDATE", description = "버전 등 기대 조건이 일치하는 행만 수정해 충돌을 원자적으로 확인하는 쿼리입니다." }]
+++
