+++
title = "자료구조와 알고리즘"
description = "자료구조의 특성과 문제 해결 복잡도"
layout = "interview"
type = "page"
standalone = true
index = 2
robots = "noindex, nofollow, noarchive"
theory_source = "assets/study-notes/data-structures.md"

[build]
list = "local"
publishResources = false
render = "always"

[sitemap]
disable = true

[[cards]]
subcategory = "선형 자료구조"
difficulty = "초급"
question = "배열과 연결 리스트의 장단점을 비교해보세요."
answer = "배열은 연속된 메모리를 사용해 인덱스 접근이 O(1)이고 캐시 효율이 좋습니다. 연결 리스트는 노드를 포인터로 연결해 위치를 알고 있을 때 삽입·삭제가 O(1)이지만 임의 접근은 O(n)입니다."
explanation = "연결 리스트도 삽입 위치를 찾는 과정까지 포함하면 O(n)이 될 수 있습니다. 실제 성능은 Big-O뿐 아니라 메모리 지역성, 포인터 오버헤드와 할당 비용의 영향을 받으므로 워크로드를 기준으로 선택해야 합니다."
points = ["배열 임의 접근 O(1), 연결 리스트 O(n)", "연결 리스트 노드는 추가 포인터 메모리를 사용한다.", "현대 CPU에서는 캐시 지역성이 중요하다."]
terms = [{ name = "임의 접근", description = "순서대로 탐색하지 않고 인덱스로 원하는 위치에 바로 접근하는 방식입니다." }, { name = "메모리 지역성", description = "가까운 메모리를 연속해서 접근할수록 CPU 캐시 효율이 높아지는 성질입니다." }]

[[cards]]
subcategory = "해시"
difficulty = "중급"
question = "해시 충돌은 왜 발생하며 어떻게 해결하나요?"
answer = "서로 다른 키가 같은 버킷 위치로 매핑될 때 해시 충돌이 발생합니다. 체이닝은 버킷마다 여러 항목을 저장하고, 개방 주소법은 다른 빈 버킷을 탐색해 저장합니다."
explanation = "좋은 해시 함수와 적절한 용량으로 키를 고르게 분산해야 합니다. 적재율이 높아지면 충돌과 탐색 비용이 증가하므로 리사이징이 필요합니다. 평균 조회는 O(1)이지만 충돌이 극단적으로 몰리면 O(n)이 될 수 있습니다."
points = ["충돌은 해시 테이블에서 피할 수 없다.", "적재율이 성능에 영향을 준다.", "키의 동등성 비교와 해시 규칙은 일치해야 한다."]
terms = [{ name = "해시 충돌", description = "서로 다른 키가 같은 해시 버킷 위치로 매핑되는 현상입니다." }, { name = "적재율", description = "해시 테이블 용량 대비 저장된 항목 수의 비율입니다." }]

[[cards]]
subcategory = "복잡도"
difficulty = "고급"
question = "시간 복잡도만으로 알고리즘의 실제 성능을 판단할 수 없는 이유는 무엇인가요?"
answer = "Big-O는 입력 크기가 커질 때 증가율의 상한을 표현하며 상수 계수, 메모리 접근 패턴과 실제 입력 분포를 생략합니다. 같은 복잡도라도 구현과 환경에 따라 성능이 크게 다를 수 있습니다."
explanation = "O(n log n) 알고리즘이 작은 입력에서는 단순한 O(n²) 알고리즘보다 느릴 수 있습니다. 캐시 적중률, 분기 예측, 할당, 병렬화와 최악·평균·상환 복잡도를 함께 살피고 벤치마크로 실제 병목을 검증해야 합니다."
points = ["복잡도는 성장률을 비교하는 도구다.", "평균·최악·상환 복잡도를 구분한다.", "최종 판단은 대표 데이터 벤치마크로 검증한다."]
terms = [{ name = "Big-O", description = "입력 크기가 증가할 때 시간이나 공간 사용량의 증가율 상한을 표현하는 표기법입니다." }, { name = "상환 복잡도", description = "연속된 여러 연산의 전체 비용을 연산당 평균으로 나누어 분석한 복잡도입니다." }]

[[cards]]
subcategory = "선형 자료구조"
difficulty = "중급"
question = "스택과 큐의 차이와 각각의 활용 사례를 설명해보세요."
answer = "스택은 나중에 넣은 데이터를 먼저 꺼내는 LIFO 구조이고, 큐는 먼저 넣은 데이터를 먼저 꺼내는 FIFO 구조입니다. 스택은 함수 호출, 괄호 검사와 되돌리기에 사용하고 큐는 작업 대기열, 메시지 처리와 BFS에 사용합니다."
explanation = "두 구조 모두 배열이나 연결 리스트로 구현할 수 있지만 제거 위치와 보장하는 순서가 다릅니다. 큐를 배열의 앞에서 계속 삭제하면 이동 비용이 생길 수 있으므로 원형 버퍼나 head 인덱스를 사용합니다. 동시 환경에서는 순서뿐 아니라 블로킹과 용량 제한도 설계해야 합니다."
points = ["스택은 LIFO, 큐는 FIFO다.", "자료구조 선택은 필요한 처리 순서에서 시작한다.", "구현체에 따라 연산의 실제 비용이 달라진다."]
terms = [{ name = "LIFO", description = "마지막에 들어온 항목이 가장 먼저 나가는 순서입니다." }, { name = "FIFO", description = "먼저 들어온 항목이 가장 먼저 나가는 순서입니다." }, { name = "원형 버퍼", description = "고정 배열의 끝과 시작을 논리적으로 연결해 공간을 재사용하는 큐 구현입니다." }]

[[cards]]
subcategory = "선형 자료구조"
difficulty = "고급"
question = "동적 배열의 append가 평균 O(1)이라고 말하는 이유를 설명해보세요."
answer = "대부분의 append는 남은 공간에 값을 기록하므로 O(1)입니다. 용량이 부족할 때만 더 큰 배열을 할당하고 기존 원소를 O(n)에 복사합니다. 용량을 기하급수적으로 늘리면 여러 append에 걸친 전체 복사 비용이 선형이므로 연산당 상환 비용은 O(1)입니다."
explanation = "매번 한 칸씩만 늘리면 n개를 추가할 때 복사량이 1+2+…+n이 되어 O(n²)이 됩니다. 보통 용량을 일정 배수로 키워 재할당 횟수를 줄입니다. 상환 O(1)은 특정 append 하나가 항상 O(1)이라는 뜻이 아니며 재할당 시 포인터와 메모리 사용이 바뀔 수 있습니다."
points = ["최악의 단일 append는 O(n)일 수 있다.", "기하급수적 증가는 전체 복사 비용을 O(n)으로 제한한다.", "상환 분석과 평균 입력 분석은 다른 개념이다."]
terms = [{ name = "동적 배열", description = "용량이 부족할 때 더 큰 연속 메모리로 확장되는 배열입니다." }, { name = "상환 분석", description = "비싼 연산이 드물게 발생할 때 연산 시퀀스 전체 비용을 나누어 계산하는 분석입니다." }]

[[cards]]
subcategory = "해시"
difficulty = "초급"
question = "해시 테이블이 평균적으로 O(1) 조회를 제공하는 원리를 설명해보세요."
answer = "해시 함수로 키를 정수값으로 변환하고 그 값을 이용해 저장할 버킷 위치를 바로 계산합니다. 키가 버킷에 고르게 분산되고 적재율이 적절하면 비교해야 할 후보가 적어 삽입과 조회를 평균 O(1)에 수행할 수 있습니다."
explanation = "조회할 때도 같은 해시 함수로 동일한 버킷을 찾아간 뒤 실제 키 동등성을 확인합니다. 충돌이 많거나 공격적으로 한 버킷에 키가 몰리면 성능이 저하됩니다. 그래서 구현체는 적재율이 임계값을 넘으면 더 큰 테이블로 리해시합니다."
points = ["해시값만 같다고 키가 같은 것은 아니다.", "평균 O(1)은 충돌 분산을 전제로 한다.", "리해시는 일시적으로 O(n) 비용이 든다."]
terms = [{ name = "해시 함수", description = "임의 길이의 키를 고정 범위의 정수 해시값으로 변환하는 함수입니다." }, { name = "버킷", description = "해시값으로 선택되는 저장 공간의 단위입니다." }, { name = "리해시", description = "테이블 크기를 바꾸고 모든 키의 저장 위치를 다시 계산하는 작업입니다." }]

[[cards]]
subcategory = "해시"
difficulty = "고급"
question = "분산 캐시에서 일관된 해싱을 사용하는 이유를 설명해보세요."
answer = "일반적인 나머지 연산으로 서버를 선택하면 서버 수가 바뀔 때 대부분의 키 위치가 변경됩니다. 일관된 해싱은 서버와 키를 해시 링에 배치해 서버 추가·제거 시 인접한 일부 키만 이동하도록 하여 캐시 미스와 재분배 비용을 줄입니다."
explanation = "서버마다 여러 가상 노드를 링에 배치하면 키 분포의 불균형을 완화할 수 있습니다. 일관된 해싱이 데이터 복제, 장애 감지와 동시 업데이트까지 해결하는 것은 아닙니다. 실제 시스템에서는 복제 계수와 서버 용량 가중치도 함께 고려합니다."
points = ["노드 변경 시 이동하는 키 범위를 줄인다.", "가상 노드로 분포를 균등하게 만든다.", "복제와 장애 복구는 별도 설계가 필요하다."]
terms = [{ name = "일관된 해싱", description = "노드 집합이 변할 때 재배치되는 키를 최소화하는 분산 해싱 기법입니다." }, { name = "해시 링", description = "해시 공간의 끝과 시작을 연결한 원형 좌표 공간입니다." }, { name = "가상 노드", description = "하나의 실제 서버를 해시 링의 여러 위치에 표현하는 논리 노드입니다." }]

[[cards]]
subcategory = "복잡도"
difficulty = "초급"
question = "O(1), O(log n), O(n)의 차이를 예시와 함께 설명해보세요."
answer = "O(1)은 입력 크기와 관계없이 일정한 단계가 필요한 배열 인덱스 접근, O(log n)은 매 단계 탐색 범위를 절반으로 줄이는 이분 탐색, O(n)은 모든 원소를 한 번 확인하는 선형 탐색의 증가율입니다."
explanation = "Big-O는 정확한 실행 시간이 아니라 입력 증가에 따른 지배적인 성장률을 비교합니다. O(1)도 느린 I/O를 포함할 수 있고 O(n)도 작은 n에서는 충분히 빠를 수 있습니다. 문제의 최대 입력과 상수 비용을 함께 고려해야 합니다."
points = ["입력 크기가 커질 때의 증가율을 비교한다.", "낮은 복잡도가 언제나 실제로 더 빠른 것은 아니다.", "최대 입력에서 허용 가능한 연산량을 추정한다."]
terms = [{ name = "상수 시간", description = "입력 크기가 변해도 연산 단계 수가 일정한 O(1) 연산입니다." }, { name = "로그 시간", description = "입력이 배수로 늘 때 연산 횟수가 일정량만 증가하는 O(log n) 성장률입니다." }]

[[cards]]
subcategory = "복잡도"
difficulty = "중급"
question = "시간과 공간의 트레이드오프를 설명하고 사례를 들어보세요."
answer = "추가 메모리를 사용해 중간 결과나 조회 구조를 저장하면 반복 계산을 줄여 실행 시간을 단축할 수 있습니다. 예를 들어 해시 맵으로 값을 저장하면 O(n²) 중첩 탐색을 O(n) 시간으로 줄이는 대신 O(n) 공간을 사용합니다."
explanation = "메모이제이션, 인덱스와 캐시가 대표적인 사례입니다. 반대로 메모리가 제한되면 결과를 스트리밍하거나 제자리 알고리즘을 사용하지만 계산과 코드 복잡성이 늘 수 있습니다. 입력 크기, 메모리 제한과 재사용 빈도를 기준으로 균형을 선택합니다."
points = ["더 빠른 조회는 보통 저장 공간과 갱신 비용을 요구한다.", "공간 복잡도에는 입력 외의 보조 공간을 구분한다.", "제약 조건을 보고 최적화 목표를 정한다."]
terms = [{ name = "트레이드오프", description = "한 특성을 개선할 때 다른 비용이나 특성을 일부 희생해야 하는 관계입니다." }, { name = "메모이제이션", description = "함수의 계산 결과를 저장해 같은 입력의 재계산을 피하는 기법입니다." }, { name = "제자리 알고리즘", description = "입력 크기에 비례하는 추가 메모리 없이 입력 공간을 활용하는 알고리즘입니다." }]
+++
